import { FilterByConditionPipe } from './filter-by-condition.pipe';

describe('FilterByConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
